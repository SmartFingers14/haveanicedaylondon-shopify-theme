<section style="padding:80px 24px">
  <div class="container" style="max-width:820px">
    <div class="section-head">
      <div>
        <span class="eyebrow">Page</span>
        <h2 class="h2 reveal">{{ page.title }}</h2>
      </div>
    </div>
    <div class="rte" style="font-size:16px;line-height:1.7;color:var(--ink-soft)">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
